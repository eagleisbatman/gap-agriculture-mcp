# ==============================================
# GAP Agriculture MCP Server - Environment Configuration
# ==============================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ----------------------------------------------
# GAP API Configuration (Required)
# ----------------------------------------------
# Get your API token from Tomorrow Now: https://tomorrownow.org
# This token is required for accessing weather data from the GAP API
GAP_API_TOKEN=your_gap_api_token_here

# GAP API base URL (usually doesn't need to be changed)
GAP_API_BASE_URL=https://gap.tomorrownow.org/api/v1

# ----------------------------------------------
# Server Configuration
# ----------------------------------------------
# Port for the MCP server to listen on
# Railway and other cloud platforms will override this automatically
PORT=3000

# Node environment (development, production, or test)
NODE_ENV=development

# ----------------------------------------------
# CORS Configuration
# ----------------------------------------------
# Comma-separated list of allowed origins for CORS
# Use * for development, specify exact domains for production
# Example: https://your-app.vercel.app,https://your-domain.com
ALLOWED_ORIGINS=*

# ----------------------------------------------
# Optional: Logging
# ----------------------------------------------
# Log level: error, warn, info, debug
LOG_LEVEL=info
